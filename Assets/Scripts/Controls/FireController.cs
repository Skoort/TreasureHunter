using UnityEngine;

// Ideas: Drill, Mining Laser, Pistol, Assault Rifle, Grenade (take it out, hold it behind you, charge up where you want to throw, and release)
// Perhaps implement placing blocks as some sort of alt fire.
public class FireController : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
